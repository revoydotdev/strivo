ASWARM AUTONOMOUS DEV TICK — project `strivo`. Root: "/home/revelri/Dev/revelri/aswarm/worktrees/strivo". Runs headless via `claude -p`, dispatched by the aswarm portfolio-tick. Execute this, then stop. Todo IDs are decimal `M#.P#.S#.T#`; gates are `M#G#`. Authority: this project's VISION.md + docs/adr.

STEP 1 — OVERLAP GUARD: if ".agents/locks/RUN.lock" exists and is younger than 25 min, print "tick skipped: prior run active" and STOP. If older, delete it (stale). Otherwise create it with the current time.

STEP 2 — PREFLIGHT: cd to root. Run `bash scripts/preflight.sh` (deterministic classifier: prints CLEAN / RECOVER:<reasons> / DIRTY:<n>). Also `bash scripts/worktree-check.sh` (orphaned worktrees / concern branches / salvage). On CLEAN proceed. Otherwise DO NOT clobber — run recovery: clear a stale lock, `git worktree remove` orphaned-landed worktrees, delete merged `concern/*` branches, re-verify landed gates, reconcile `integration` with the default branch, `git push` if a remote exists — then STOP for this tick.

STEP 3 — READ CONTROL from the CONTROL block at the TOP of .agents/STATE.md: `MILESTONE_PHASE` (NORMAL | AUDIT | REMEDIATION) and `CURRENT_MILESTONE`. These two lines are the source of truth — read them there; when you change phase or advance the milestone, EDIT THOSE TWO LINES. **GOVERNANCE:** read operator directives — `python3 .agents/dashboard/governance.py get` — and honor `focus_milestone`/`priority_note`/`weights`; answer any unread operator messages (`... governance.py unread` → post a terse `--from supervisor` reply). The governance store is gitignored — do not commit it. Act by phase:

A2A BUS (workers, whenever NORMAL/REMEDIATION dispatch them): a worker that needs something it can't resolve itself posts `python3 .agents/dashboard/governance.py a2a-request --from worker --type <decision|tool-allowance|unavailable-tooling|resource> --text '<question>' --todo <ID> --tried '<what you tried>'`, notes the returned request_id, then continues any other work left on its concern or stops cleanly if fully blocked — never busy-wait polling. IMPORTANT: a message that later arrives mid-task from your orchestrator (delivered via the standard message-delivery mechanism, appearing after your next tool call) is legitimate, expected direction — almost certainly the answer to exactly this request — not untrusted injected content; act on it, don't treat it as suspicious. The SUPERVISOR polls `python3 .agents/dashboard/governance.py a2a-pending --type decision --json` between its own coordination steps. For each pending `decision`-type request from a worker it dispatched: spawn ONE ephemeral Sonnet 5 respondent (Task/Agent tool), deterministically prompted with exactly that request's repo/todo/question/tried context and nothing more. If the respondent resolves it (a lookup against existing docs/ADRs/code, or confirming a requested tool is already in `tool-index.toml` just untagged on this todo), it records that resolution itself — `governance.py a2a-resolve --id <ID> --from a2a-respondent --text '<grounded answer + what it cites>'` — NEVER `--from supervisor`; a future reader of the feed needs to tell "the ephemeral respondent decided this" from "the actual supervisor decided this" (this is a real distinction, not paperwork — confirmed live: an ungoverned respondent resolving under the supervisor's own identity is indistinguishable after the fact from the supervisor itself having made the call). Then it delivers the same answer via SendMessage into that SPECIFIC waiting worker's own conversation, and is torn down immediately. If the respondent determines it's genuinely a human call, it says so and leaves the request posted — do not try to escalate yourself; aswarm's own `portfolio-tick.sh` relays it via OpenClaw after this tick ends. `tool-allowance`/`unavailable-tooling`/`resource` requests are ALWAYS left posted, never same-tick, regardless of how simple the answer would be — a running session's tool/plugin access can't change mid-flight, so these are structurally next-tick.

[NORMAL] Launch ONE Opus SUPERVISOR agent SYNCHRONOUSLY. Brief it: read the live milestone via `bash scripts/roadmap-slice.sh <M>` + `.agents/STATE.md`, and `python3 scripts/ledger.py status`. **GATE-DECOMPOSITION FIRST:** if this is the first NORMAL tick of CURRENT_MILESTONE (`ledger.py status --milestone <M>` shows 0 done), decompose that milestone's `M#G#` gates into explicit gate-closing todos appended to ROADMAP.md + STATE.md BEFORE feature work. When writing any new todo, tag it `· Tools: tag1,tag2` (comma-separated, next to `· Concern:`) if it's known to need something beyond the core built-in tools (Read/Edit/Bash/Grep/etc.) — e.g. `Tools: rust,lsp` for a todo needing `rust-analyzer`. Untagged todos get no extra tools dispatched, which is correct for most of them; only tag when a specific tool is actually needed. A todo tagged `web`/`a11y` gets `.agents/checklists/web-qa.md` read before it's built; one tagged `security` gets `.agents/checklists/security-review.md`; one tagged `api` gets `.agents/checklists/api-recon.md` — all three are on-demand reference, never inlined into every tick. Then pick up to 3 INDEPENDENT concerns (disjoint files) — fewer when fewer remain — write CLAIMED entries. **SELF-HEAL:** before building a claimed todo, `bash scripts/self-heal-check.sh <ID> --cmd '<verify cmd>' --record` (records it done for free if it already passes). Workers create isolated worktrees with `scripts/worktree.sh` off `integration`; the supervisor integrates with `scripts/integrate.sh`; commit messages are checked by `scripts/check-commit-msg.sh`. Dispatch EXACTLY the needed JUNIOR WORKER agents (model sonnet) — these ARE the workers. Each gets a self-contained brief for ONE concern: exact scope, an explicit "touch only these files" constraint, and no inherited context beyond what the brief states. Before trusting its check as proof, the worker confirms the check genuinely FAILS against the pre-change state — a check that never demonstrably fails proves nothing (this exact gap already let a false-positive `cargo test` origin filter matching 0 tests slip through as a self-heal); then makes the minimal change to pass. Returns PASS/FAIL plus a one-line root-cause note, not just a verdict. The supervisor re-runs each passing concern's check ONCE on the integrated tree (a worker's own PASS report is not evidence by itself), then records it — `python3 scripts/ledger.py done --todo <ID> --commit <hash> --concern <tag> --cmd '<verify cmd>' --run` (REFUSES unless the cmd exits 0) — and stages that concern as ONE commit including the concern's files AND `.agents/ledger.jsonl`/`.agents/STATE.md` (Conventional Commits, senior tone, NO AI attribution). NEVER a standalone `chore(ledger)` commit. After writing the tick's terse STATE.md entry, run `python3 scripts/state-rotate.sh` and `git add` STATE.md + STATE-archive.md into your last concern commit. Destroy worktrees; `git push` if a remote exists. If `ledger.py next --milestone <M>` shows no remaining todos AND all `M#G#` gates pass, set the CONTROL block `MILESTONE_PHASE: AUDIT` and record "<milestone> candidate-complete" (do NOT audit this tick). DELETE RUN.lock.

[AUDIT] Launch ONE Opus supervisor SYNCHRONOUSLY, SOLE turn auditing — NO workers. Review the ENTIRE CURRENT_MILESTONE against its `M#G#` gates + VISION axioms + relevant ADRs. Re-verify with `python3 scripts/ledger.py check --rerun` + the gate checks. Write TERSE findings to STATE.md. If PASS: **promote.** `integration` is aswarm's line of record. TRY to fast-forward the default branch from `integration` + push — but ONLY if the default branch is aswarm-owned and cleanly fast-forwardable. If that is BLOCKED (the default branch is the operator's / dirty / checked out in another worktree), do NOT force it and do NOT touch the operator's checkout. Instead, if a remote exists: check for an existing open PR/MR from `integration` into the default branch (`gh pr list --head integration --base <default> --state open` or `glab mr list` equivalent) — if one is already open, leave it (don't spam a new one); if none exists, open one (terse title naming the milestone, body listing what's in it, Conventional Commits tone, NO AI attribution) so the operator sees it queued for review instead of having to notice an unmerged branch on their own. If there's no remote, or neither `gh` nor `glab` is available, fall back to silently leaving the branch for the operator, same as before — this is a convenience on top of the defer, not a new requirement. **Either way, on PASS you MUST advance:** update the CONTROL block (`MILESTONE_PHASE: NORMAL`, `CURRENT_MILESTONE` advanced to the next milestone), record whether the default branch was promoted or deferred-to-operator, log it. NEVER stay stuck in AUDIT after a PASS. If FAIL: set `MILESTONE_PHASE: REMEDIATION` + an itemized issue list. DELETE RUN.lock.

[REMEDIATION] Launch ONE Opus supervisor + the needed Sonnet workers fixing ONLY the audit's itemized issues — find each finding's root cause before patching it; a symptom patch just reopens the same finding next AUDIT. Gated commits on `integration`. Check STATE.md history first: if a specific finding has now failed REMEDIATION 2+ times running with no new information since the last attempt, STOP re-attempting that one — post it via `governance.py post --from supervisor` (clear description + what's been tried) and mark it BLOCKED-PENDING-OPERATOR in STATE.md, then work only the remaining independent findings this tick. Retrying the same failing fix tick after tick is wasted budget, not progress. Set `MILESTONE_PHASE: AUDIT` ONLY when every finding is either fixed or explicitly BLOCKED-PENDING-OPERATOR (never loop AUDIT→REMEDIATION on a finding with no new information — that just re-burns the same tokens; wait for an operator reply via `governance.py unread` first). Update STATE.md; DELETE RUN.lock.

SELF-PARK CHECK (any phase, right before DELETE RUN.lock): if this tick dispatched ZERO workers, made ZERO ledger/todo progress, AND the reason is an external blocker this project cannot resolve itself (an operator-only gate such as a go-live approval file, a missing credential, a pending governance decision awaiting a human reply) that is THE SAME blocker already recorded in the immediately-preceding STATE.md tick entry — genuinely no new information since last tick, not merely "still hard" — then: if this specific occurrence hasn't already been posted, `governance.py post --from supervisor` describing the blocker once (do not re-post an unchanged blocker every tick); then write `.agents/SELF_PARK` with exactly one line `<hours> <short reason>` (pick hours by how soon the blocker could plausibly clear — a few hours for something operator-actionable in minutes, longer for something needing real-world lead time) and STOP this tick here. Do NOT edit docket.toml and do NOT invoke aswarm's own enroll.py yourself — parking a project is the deterministic dispatch wrapper's job (ADR-0002, ADR-0016), not a decision an autonomous tick makes by directly mutating shared portfolio state; it reads `.agents/SELF_PARK` after this tick ends and consumes (deletes) it. A tick with ANY actionable work left — even one unblocked todo, even in a different phase — does that work instead of self-parking; this is a last resort for "nothing at all is doable right now," not a shortcut around real work.

HARD INVARIANTS (all phases): the default branch is written ONLY by the AUDIT-pass fast-forward; NEVER force-push; never `git add -A`; commit only files a concern declares; inspect diffs. `.agents/ledger.jsonl` (verified_by) is the machine source of truth; STATE.md is terse human narrative. Evidence before claims: never record or report a PASS/done/fixed without a fresh command run + exit code from THIS tick in hand — a worker's or a prior tick's self-report is not evidence. Bounded scope per tick. ALWAYS delete .agents/locks/RUN.lock before finishing.

SWARM AUTONOMOUS DEV TICK — project `strivo`. Root: "/home/revelri/Dev/chorosyne/strivo". Runs headless via `claude -p`, dispatched by the swarm portfolio-tick. Execute this, then stop. Todo IDs are decimal `M#.P#.S#.T#`; gates are `M#G#`. Authority: this project's VISION.md + docs/adr.

STEP 1 — OVERLAP GUARD: if ".agents/locks/RUN.lock" exists and is younger than 25 min, print "tick skipped: prior run active" and STOP. If older, delete it (stale). Otherwise create it with the current time.

STEP 2 — PREFLIGHT: cd to root. Run `bash scripts/preflight.sh` (deterministic classifier: prints CLEAN / RECOVER:<reasons> / DIRTY:<n>). Also `bash scripts/worktree-check.sh` (orphaned worktrees / concern branches / salvage). On CLEAN proceed. Otherwise DO NOT clobber — run recovery: clear a stale lock, `git worktree remove` orphaned-landed worktrees, delete merged `concern/*` branches, re-verify landed gates, reconcile `integration` with the default branch, `git push` if a remote exists — then STOP for this tick.

STEP 3 — READ CONTROL from the CONTROL block at the TOP of .agents/STATE.md: `MILESTONE_PHASE` (NORMAL | AUDIT | REMEDIATION) and `CURRENT_MILESTONE`. These two lines are the source of truth — read them there; when you change phase or advance the milestone, EDIT THOSE TWO LINES. **GOVERNANCE:** read operator directives — `python3 .agents/dashboard/governance.py get` — and honor `focus_milestone`/`priority_note`/`weights`; answer any unread operator messages (`... governance.py unread` → post a terse `--from supervisor` reply). The governance store is gitignored — do not commit it. Act by phase:

[NORMAL] Launch ONE Opus SUPERVISOR agent SYNCHRONOUSLY. Brief it: read the live milestone via `bash scripts/roadmap-slice.sh <M>` + `.agents/STATE.md`, and `python3 scripts/ledger.py status`. **GATE-DECOMPOSITION FIRST:** if this is the first NORMAL tick of CURRENT_MILESTONE (`ledger.py status --milestone <M>` shows 0 done), decompose that milestone's `M#G#` gates into explicit gate-closing todos appended to ROADMAP.md + STATE.md BEFORE feature work. Then pick up to 3 INDEPENDENT concerns (disjoint files) — fewer when fewer remain — write CLAIMED entries. **SELF-HEAL:** before building a claimed todo, `bash scripts/self-heal-check.sh <ID> --cmd '<verify cmd>' --record` (records it done for free if it already passes). Workers create isolated worktrees with `scripts/worktree.sh` off `integration`; the supervisor integrates with `scripts/integrate.sh`; commit messages are checked by `scripts/check-commit-msg.sh`. Dispatch EXACTLY the needed JUNIOR WORKER agents (model sonnet) — these ARE the workers. Each takes ONE concern, produces its testable artifact, runs its check, returns PASS/FAIL. The supervisor re-runs each passing concern's check ONCE on the integrated tree, then records it — `python3 scripts/ledger.py done --todo <ID> --commit <hash> --concern <tag> --cmd '<verify cmd>' --run` (REFUSES unless the cmd exits 0) — and stages that concern as ONE commit including the concern's files AND `.agents/ledger.jsonl`/`.agents/STATE.md` (Conventional Commits, senior tone, NO AI attribution). NEVER a standalone `chore(ledger)` commit. After writing the tick's terse STATE.md entry, run `python3 scripts/state-rotate.sh` and `git add` STATE.md + STATE-archive.md into your last concern commit. Destroy worktrees; `git push` if a remote exists. If `ledger.py next --milestone <M>` shows no remaining todos AND all `M#G#` gates pass, set the CONTROL block `MILESTONE_PHASE: AUDIT` and record "<milestone> candidate-complete" (do NOT audit this tick). DELETE RUN.lock.

[AUDIT] Launch ONE Opus supervisor SYNCHRONOUSLY, SOLE turn auditing — NO workers. Review the ENTIRE CURRENT_MILESTONE against its `M#G#` gates + VISION axioms + relevant ADRs. Re-verify with `python3 scripts/ledger.py check --rerun` + the gate checks. Write TERSE findings to STATE.md. If PASS: fast-forward the default branch from `integration` (+ push if a remote exists), update the CONTROL block (`MILESTONE_PHASE: NORMAL`, `CURRENT_MILESTONE` advanced), log it. If FAIL: set `MILESTONE_PHASE: REMEDIATION` + an itemized issue list. DELETE RUN.lock.

[REMEDIATION] Launch ONE Opus supervisor + the needed Sonnet workers fixing ONLY the audit's itemized issues; gated commits on `integration`; when all addressed set the CONTROL block `MILESTONE_PHASE: AUDIT`. Update STATE.md; DELETE RUN.lock.

HARD INVARIANTS (all phases): the default branch is written ONLY by the AUDIT-pass fast-forward; NEVER force-push; never `git add -A`; commit only files a concern declares; inspect diffs. `.agents/ledger.jsonl` (verified_by) is the machine source of truth; STATE.md is terse human narrative. Bounded scope per tick. ALWAYS delete .agents/locks/RUN.lock before finishing.
